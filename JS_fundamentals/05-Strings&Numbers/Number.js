// Numbers in JS
let num = 17; // creates a primitive number 
let num_2 = new Number(17) // creates a Number object (an object wrapper around a number).


 // 1.  toFixed()
 let number = 123.1134
console.log(number.toFixed(2)) // 123.11 (after decimal how much values)
// 2. toPrecision()
console.log(number.toPrecision(3)) // focused on the left before decimal


